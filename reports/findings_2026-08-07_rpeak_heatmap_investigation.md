# R波heatmap検出: R/T混同問題の調査ログ (2026-08-06〜)

> 進捗報告書(`progress_report_2026-08-05_presentation.md`)III-6bの詳細版・作業ログ。
> 結論だけでなく「何を試して何が効かなかったか」を時系列で残す。

## 発端

heatmap出力が1心拍につき隣接する2つの山を出しており、2つ目がT波の位置と一致していた。
`continuous_stitched_reconstruction.png`で正解R波ラベル自体がT波位置に付いていた例も発見。
原因は教師ラベル生成に使っていた検出器(`detect_r_peaks`, z-score閾値+`find_peaks`, QRS強調
フィルタなし)がT波を第二のR波候補として誤検出すること。

## 応急対応: `extract_peaks_paired_dedup`

しきい値を下げ、近接する2候補は前者(時間的に早い方)だけを残す後処理。
**旧ラベル学習チェックポイントに対しては**モデルごとに効果が違う:
Anchor CNN F1 0.570→0.639(改善)、GNN F1 0.497→0.513(dedupのみ、しきい値据え置き時)。

## 恒久対応: NeuroKit2への切り替え

`detect_r_peaks_neurokit`(Makowski et al. 2021)で全91トライアルを再ラベル付け。
GNNは正解を都度計算するため先に本番50epoch再学習が完了、公平比較(同一のneurokit GTで
新旧チェックポイントを評価):

| チェックポイント | F1 | RR-MAE | RMSSD-MAE |
|---|---|---|---|
| 旧(旧ラベル学習) | 0.510 | 10.27 ms | 20.90 ms |
| 新(NeuroKit2ラベル学習) | **0.551** | 10.69 ms | **16.27 ms** |

**重要な副作用**: 応急対応(paired_dedup)は恒久対応後のモデルには**有害**(F1 0.551→0.227)。
R-T混同という誤ラベル起因の症状を抑える処置は、根本原因解消後は単なるノイズ増加になる。
**応急対応と恒久対応は同時適用しない。**

## 後処理アルゴリズムの比較(同一heatmapキャッシュ, config274)

`compare_peak_postprocessing.py`でheatmap計算を1回だけキャッシュし、複数の後処理を
同一入力で公平比較。

| 後処理 | F1 | RR-MAE | precision | recall |
|---|---|---|---|---|
| 旧手法(閾値0.3) | 0.551 | 10.69 ms | 0.716 | 0.486 |
| paired_dedup(閾値0.10) | 0.227 | 13.93 ms | - | - |
| リズム一貫性DP(Ellis 2007型) | 0.558 | 12.71 ms | 0.651 | 0.512 |
| Pan-Tompkins型(既定パラメータ) | 0.303 | 13.67 ms | 0.189 | 0.803 |
| **Pan-Tompkins型(再校正後)** | **0.687** | 12.00 ms | 0.683 | 0.705 |

### Pan-Tompkins型の原因診断と再校正

既定パラメータでの悪化(precision 0.189)の原因を診断: heatmapの真のR波位置での予測値は
中央値0.25程度で、ノイズ床(平均0.14)との分離が乏しい。Pan-Tompkinsの忘却係数・しきい値比
(原著0.25)はQRS前処理後の高SN比信号を前提にしており、この入力には過剰発火する。
`threshold_frac`をパラメータ化し`sweep_adaptive_searchback_params.py`(GPU不要、CPU後処理
のみ)でグリッドサーチした結果、`candidate_height=0.20, threshold_frac=0.8,
searchback_threshold_frac=0.6`でF1=0.687を達成。**これまでに試した後処理の中で最良。**

## 「自信度が低い原因」の切り分け (2026-08-07)

ユーザ質問: heatmapの自信度上限(~0.3-0.4)が低いのは、neurokit2再ラベル(我々の変更)が
原因か、学習不足か？

**単一窓 vs 複数窓融合の比較**: 真のR波を含む単一の非融合窓でも予測値は0.33-0.39程度
(融合による平滑化が主因ではない)。

**旧モデル自身の自己評価** (旧ラベル・legacy検出器で学習したモデルを、そのモデル自身が
学習に使ったlegacy検出器の正解に対して評価):

| | 真のR波位置でのheatmap値 中央値 | 0.3未満の割合 |
|---|---|---|
| 旧モデル(旧ラベル,自己評価) | 0.249 | 71.8% |
| 新モデル(NeuroKit2ラベル) | 0.247 | 74.6% |

**結論: ほぼ同一。低い自信度はneurokit2再ラベルが原因ではなく、T波混同问题以前から
存在していた特性**（BCE+狭いガウシアン教師(σ=10ms)の学習ダイナミクス、または
レーダ信号とECG R波の間の本質的なタイミング不確実性に起因すると推測される。未検証）。

## 派生案の検討 (2026-08-07)

### 案1: R波+T波を両方教師に含める

`detect_r_and_t_peaks_neurokit`(`nk.ecg_delineate`, method="dwt")を実装。
フィージビリティ確認: MMECGデータでT波検出192/192件成功、R→T間隔(QT間隔相当)は
245-255ms(percentile 10-90)と非常に安定。

**可視化での裏付け**: `preprocessing_inspection/raw_vs_bandpass_vs_ecg.png` —
レーダ生波形にはR波位置に対応する大きな二相性スパイクと、その直後(ECGのT波とほぼ同じ
タイミング)に小さな山のクラスタが実際に存在することを目視確認。**モデルが「間違って」
T波を検出しているのではなく、レーダ信号に物理的に対応する変化が実在する**ことが示唆される。

`rpeak_detector: neurokit_rt`をデータパイプラインに追加、config276で8epochプローブ実施。

**8epochプローブ結果(有望)**: 旧手法F1=0.519/RR-MAE=10.43ms、paired_dedupでF1=0.531/
RR-MAE=10.41ms(R波単独教師の本番モデル274のRR-MAE 10.69msに、わずか8epochで匹敵)。

**本番50epoch結果(config277, 悪化)**: 8epochプローブの好調さとは裏腹に、50epoch本番では
**明確に悪化**した:

| 後処理 | F1 | RR-MAE | RMSSD-MAE |
|---|---|---|---|
| 旧手法 | 0.437 | 11.82 ms | 48.74 ms |
| paired_dedup | 0.491 | 11.83 ms | 43.69 ms |
| adaptive_searchback | 0.461 | 13.17 ms | 57.02 ms |

R波単独教師の274(F1 0.551/RR-MAE 10.69ms/RMSSD-MAE 16.27ms)に全指標で劣る。学習曲線
(train_corr 0.877まで上昇、val_corr 0.19-0.24で頭打ち)から**過学習**が疑われる。
「T波を明示的に教師に含めれば学習課題が一貫する」という仮説自体は理にかなっていた
(可視化でも物理的根拠を確認)が、**実際の本番学習では逆効果**という、プローブと本番の
乖離を示す教訓的な結果。**結論: R+T統合教師は採用しない。R波単独教師のconfig274を
最良として維持する。**

### 案2: 波形推定モデルを補助信号にする案(未着手)

`ecgshape`モデルで4秒窓からECG波形を推定し、その推定波形自体のR/T構造(またはneurokit2で
推定波形上のR/T検出)をheatmap候補の選別に使う案。前提として`ecgshape`モデル自体が
R/T混同ラベルの影響を受けている可能性が高く、まずneurokit2ベースで再学習する必要がある
(未着手)。

## 前処理(バンドパスフィルタ)の検討 (2026-08-07)

**既存の知見**: SST/波形回帰パイプライン(`mmecg_singlecycle_dataset.py`)では既に
[1,25]Hzバンドパスフィルタが試され(config253/254 vs 248)、**悪化**の記録がある。
ただし別パイプライン・旧ラベルでの結果であり、直接は今回のheatmapパイプラインに
適用できない。

**現状確認**: heatmapパイプライン(`zscore_channels`, Anchor CNN/GNN共通)は現在
バンドパスフィルタを一切適用していない(z-score正規化のみ)。

**可視化**: `preprocessing_inspection/raw_vs_bandpass_vs_ecg.png`の該当窓では、
フィルタ前後で波形の見た目にほぼ差がない(このチャネル・この窓ではノイズが少なく
恩恵が小さい可能性)。ただし別のノイズが多い窓・チャネルでは異なる可能性があり、
限定的な確認に留まる。

**8epochプローブ結果(config278, R波単独教師+bandpass)**: R波単独教師の同条件8epoch
プローブ(config273, F1=0.549)と比較して**F1=0.343と明確に悪化**。HRV算出可能率も
34/44に低下(273は44/44)。一方でRR-MAE=8.96msはこれまでの全結果の中で最良値
（ただしマッチできた拍が少ない中での値であり、単純に「精度が上がった」とは言えない
点に注意）。**SST側の過去の否定的結果と方向は一致しており、本パイプラインでも
バンドパスフィルタは(少なくとも8epochでは)悪化要因**と判断。本番50epochへの
昇格は見送る（付録Aに追記）。

## GPU/CPU基盤の高速化 (2026-08-07)

- `sst_batch`: `prepare_mmecg_dataset.py`のSST計算を、チャネルごとのループから
  50chバッチ一括呼び出しに変更。数値的に完全一致を確認(max abs diff = 0.0)。
  CPU上で約8.7倍高速化(1ch/回=1.06秒/ch → 50chバッチ=6.08秒/50ch=0.12秒/ch)。
  GPU(`SSQ_GPU=1`、要cupy)はさらに約1.8倍だが、`multiprocessing`との組み合わせで
  過去にCUDA fork問題(33分デッドロック)を起こした実績があるため、リラベル付けの
  4並列ワーカーでは採用を見送り、CPUバッチ化のみ適用。
- **事故と教訓**: 完了監視に「PIDの生存」を使うウォッチャーを設定していたところ、
  手動でプロセスをkillした際に「正常終了」と誤判定し、キュージョブ(Anchor CNN学習)が
  データ未完成のまま起動してクラッシュした。**完了判定は生存確認ではなく、実際の
  成果物(この場合はトライアル数)を数える方式にすべき。** `012_anchor_cnn_neurokit_full.sh`
  を修正済み。

## Anchor CNN本番再学習の結果 (2026-08-07)

`_recover_seg_starts`のdetector不一致バグ修正後、Anchor CNN(20epoch)をneurokit2ラベルで
本番再学習。同一のneurokit GTで新旧チェックポイントを比較:

| チェックポイント\後処理 | old | adaptive_searchback | rhythmic |
|---|---|---|---|
| 旧(旧ラベル学習) F1 | **0.593** | 0.580 | 0.515 |
| 新(neurokit学習) F1 | 0.556 | 0.554 | 0.475 |
| 旧 RR-MAE | **15.36 ms** | 16.62 ms | 17.87 ms |
| 新 RR-MAE | 17.21 ms | 16.59 ms | 17.70 ms |

**GNNとは逆の結果**: GNNではneurokit再学習が明確に改善したが、Anchor CNNでは**旧ラベル
学習チェックポイントの方が全指標で優れている**。学習ログのtest_BCEはepoch4(0.1255)を
底に単調悪化しており、20epochでも早期に過学習している可能性がある。学習が速い
(1epoch=1.4秒)モデルなので、epoch数・学習率などのハイパーパラメータがneurokit
ラベル向けに再調整が必要な可能性がある(未検証、今後の課題)。

## ecgshape(波形回帰)モデルのneurokit2プローブ (2026-08-07)

8epochプローブでtest_Corr 0.799〜0.857。**III-8で確立した「入力を見ない平均形状
モデルでも相関0.883」という基準値をまだ下回っている**ため、この時点では改善したとは
言えない(R3: 相関を報告する際は必ずこの基準値と併記すること)。本番epoch数での
再学習・RMSE基準での評価が必要(未着手)。

## 前処理: EMAクラッタ除去プローブ結果 (2026-08-07)

8epochプローブでval_corrはepoch2(0.268)をピークに以後低下(0.148まで)。バンドパス
ほど明確な悪化ではないが、良好とも言えない。詳細評価は`ema_clutter_probe_eval.log`参照。

## 新規: チャネル重み付け前処理 (2026-08-07)

FMCWレーダのバイタルサイン計測分野の「チャンネル/アンテナ選択」("Cardio-Focusing"型、
心拍情報が強い空間位置を動的に特定する発想)を参考に、`channel_weighting.py`を実装。
50点それぞれの心拍帯([0.8,3]Hz)パワー比を計算し、心拍由来の変位が強い点を重視する
重み付けをRCG正規化前にかける。

**8epochプローブ結果(本日試した中で最良)**: F1=0.524(前処理なし基準0.549よりやや低い)
だが、**RR-MAE=10.32ms・RMSSD-MAE=17.39ms・SDNN-MAE=7.79msはいずれも本日試した
全プローブ(前処理なし・バンドパス・EMAクラッタ除去・R+T統合教師)の中で最良**。
本番50epoch(config281)へ昇格。

## 8epochプローブ横断比較(まとめ)

| 前処理/教師 | F1 | RR-MAE | RMSSD-MAE |
|---|---|---|---|
| なし(基準, config273) | 0.549 | 10.91 ms | 18.06 ms |
| バンドパス(config278) | 0.343 | 8.96 ms※ | — |
| EMAクラッタ除去(config279) | 0.529 | 10.57 ms | 20.52 ms |
| **チャネル重み付け(config280)** | 0.524 | **10.32 ms** | **17.39 ms** |
| R+T統合教師(config276) | 0.519〜0.531 | 10.41〜10.43 ms | 24.44〜28.26 ms |

※バンドパスのRR-MAEはマッチできた拍が少ない(HRV算出可能率34/44)中での値であり、
単純比較できない点に注意。

## R+T「過学習」の真因訂正、Anchor CNN悪化の視覚診断 (2026-08-07、ユーザ指摘を受けて)

**R+Tモデルの「過学習」は誤診断だった**: 学習ログを精査したところ、実際に保存された
`best_model.pt`はepoch36(val_corr=0.244、全50epoch中の最高値)であり、8epochプローブの
ベスト(epoch1、val_corr=0.223)とほぼ同水準だった。にもかかわらず下流タスク(F1・RR/RMSSD
精度)は大きく悪化していた。**真因は過学習ではなく、チェックポイント選択基準(heatmap全体
のPearson相関)が下流タスクの精度と乖離していたこと**。`spatial_fusion_trainer.py`に
5epochごとの周期チェックポイント保存を追加し、post-hocに下流タスクで選び直せるようにした。

**Anchor CNN悪化の視覚診断**（`diagnose_neurokit_label_placement.py`、
`neurokit_label_placement_diagnosis.png`）: ecgshapeモデルの拍単位相関が低い順に5件選び、
参照ECG+neurokit R波位置+Anchor CNN(neurokit学習)の予測heatmapを並べて目視確認した。
**ラベルはR波に正しく配置されており、T波への誤配置は見られなかった**。相関が低かった
窓は心拍数が速い(4秒窓に8〜9拍、120〜135bpm相当)ケースで、単純に単一拍の切り出しが
短く難しいことが原因だった。**Anchor CNNの悪化はラベル誤りではなく、学習の
ハイパーパラメータ(現状20epoch、既定設定)がneurokitラベルに対して早期に劣化している
ことが原因と考えられる**(test_BCEがepoch4を底に単調悪化)。エポック数・学習率の
再チューニングが今後の課題。

## チャネル重み付け本番50epoch(config281)の初回結果は無効、再学習中 (2026-08-07)

config281(8epochプローブで前処理群のうち最良だったchannel_weight)を本番50epoch化した
初回実行(キュー017)は、**periodic checkpoint修正(commit 5c90d86)がコミットされる
わずか3分前にジョブが開始していた**ため、`best_model.pt`はval_corrが最高値を
取ったepoch2(訓練のごく初期)のまま固定され、5epochごとの中間チェックポイントが
一切残らなかった。R+T(config277)と同型の「corrベースのチェックポイント選択が
下流タスクと乖離する」問題を、post-hocに検証する手段がない状態で評価するしかなかった。

`evaluate_spatial_gnn_hrv.py`に`--preprocess`フラグを追加(学習時の
`iter_peak_windows`と同じ「前処理->zscore」の順序を評価側でも再現しないと、
学習・評価間で入力分布が食い違い正しく比較できないため)した上で、このepoch2
チェックポイントを評価したところ:

| 後処理 | F1 | RR-MAE | RMSSD-MAE | SDNN-MAE |
|---|---|---|---|---|
| old (閾値0.3) | 0.381 | 10.03 ms | 20.43 ms | 10.07 ms |
| adaptive_searchback(re-tuned) | 0.649 | 12.59 ms | 25.01 ms | 8.47 ms |

274(前処理なし、adaptive_searchback、F1=0.687/RR-MAE=10.69ms/RMSSD-MAE=16.27ms)と比べ
明確に悪化している。しかしこの結果はepoch2という**訓練のごく初期のチェックポイント
に基づく比較であり、「channel_weight前処理が悪い」のか「単に学習不足のチェックポイント
しか無かった」のかを現時点では切り分けられない**。

**対応**: トレーナーは既に周期チェックポイント修正を含んでいるため、config281を
そのまま再学習した(キュー018、`281_channel_weight_full_rerun`)。完了後、
epoch 5,10,...,50の各チェックポイントを下流F1/RR-MAE/RMSSD-MAEで評価し、
最良epochで274と改めて公平比較する。

## Anchor CNN過学習への対応: weight_decay再チューニング (2026-08-07)

前節の視覚診断でラベル配置は正しいと確認済みのため、残る仮説(学習ハイパーパラメータ)
を検証する。`train_anchor_raw_cnn.py`は元々weight_decay=1e-4に固定されていたため
`RADARODE_WD`環境変数で外出しし、単一変数(CLAUDE.md R4)としてweight_decay=1e-3版を
キュー登録した(019、`best_anchor_raw_cnn_neurokit_wd1e3.pt`)。

**結果**: test_BCEの底がepoch4(0.1255)からepoch17(0.1242)へ後ろ倒しになり、
わずかにBCE自体は改善したが、下流F1=0.552は元の水準(0.554前後)から実質的に
変化しなかった。**weight_decay強化は過学習の"底"を遅らせただけで、下流精度の
決定的な改善にはつながらなかった**。Anchor CNNの悪化はweight_decayの単純な
調整では解消しない別要因(モデル容量、学習率スケジュール、あるいはneurokit
ラベル自体がAnchor CNNの帰納バイアスと相性が悪い可能性)が残っており、
優先度を下げて保留とする(現状の生産用モデルは空間GNNの274が優勢なため)。

## config281再学習(キュー018)の全チェックポイント下流評価、274との最終比較 (2026-08-07)

017の無効な結果(epoch2の未成熟チェックポイントのみ)を受け、周期チェックポイント
修正込みでconfig281を再学習(018)。全10チェックポイント(epoch4,9,...,49)+
best_model(val_corr最高=epoch2)を、`--peak_method adaptive_searchback`
(274で最良と確認済みの後処理、`--gt_detector neurokit --preprocess channel_weight`)
で下流評価した:

| checkpoint | F1 | RR-MAE | RMSSD-MAE | SDNN-MAE |
|---|---|---|---|---|
| epoch4 | **0.700** | 11.71 ms | 23.70 ms | 8.52 ms |
| epoch9 | 0.654 | 11.15 ms | 28.12 ms | 10.57 ms |
| epoch14 | 0.613 | 11.53 ms | 34.50 ms | 14.10 ms |
| epoch19 | 0.566 | 11.57 ms | 32.02 ms | 13.72 ms |
| epoch24 | 0.557 | 11.64 ms | 36.92 ms | 15.37 ms |
| epoch29 | 0.682 | 11.26 ms | 29.66 ms | 9.87 ms |
| epoch34 | 0.585 | 12.22 ms | 42.45 ms | 17.73 ms |
| epoch39 | 0.603 | 12.45 ms | 41.37 ms | 15.44 ms |
| epoch44 | 0.549 | 10.77 ms | 39.78 ms | 18.30 ms |
| epoch49 | 0.561 | 11.20 ms | 44.98 ms | 17.38 ms |
| best_model(epoch2,val_corr最高) | 0.674 | 12.24 ms | 21.34 ms | 7.25 ms |
| **274(前処理なし、比較対象)** | **0.687** | **10.69 ms** | **16.27 ms** | (未計測) |

**結論: channel_weight前処理は274(前処理なし)を上回らない。棄却する。**
最良チェックポイント(epoch4)でF1こそ0.700と274の0.687をわずかに上回るが、
RR-MAE(11.71ms vs 10.69ms)は悪化し、**RMSSD-MAE(23.70ms vs 16.27ms)は
大幅に悪化**している。RMSSD-MAEはユーザが目的上最重要と明言した指標であり、
全チェックポイントを通してどの epoch も274のRMSSD-MAE(16.27ms)を下回れて
いない(最良でもbest_model/epoch2の21.34ms)。F1がわずかに高いepochでも
RMSSD-MAEが軒並み23〜45msまで悪化する不安定さも確認された(epoch選びの
安定性という観点でも274に劣る)。

**現行の生産用構成として確定: config274(前処理なし)の空間GNN +
`extract_peaks_adaptive_searchback`(candidate_height=0.20,
threshold_frac=0.8, searchback_threshold_frac=0.6) = F1=0.687,
RR-MAE=10.69ms, RMSSD-MAE=16.27ms。**

この結果は同時に、R2(プローブ後の本番判断)の限界も示している: 280の
8epochプローブではchannel_weightがRR-MAE/RMSSD-MAEで274本番に迫ると
見えたが、本番50epochでの周期チェックポイント全数評価では一貫して
274を下回った。**短いプローブの「良さそう」は、本番の全epochを通した
安定性まで保証しない**という教訓として記録する。

## heatmap Gaussianラベル幅(sigma)プローブ: F1は大幅改善もRMSSD-MAEは悪化、棄却 (2026-08-07)

`heatmap_sigma_ms`を`iter_peak_windows`/`MMECGSpatialWindowDataset`/
`spatial_fusion_trainer`に単一変数として通し(既定10ms)、config282でsigma=20msの
8epochプローブを実施した(`build_peak_heatmap`のGaussian幅を倍にしただけ)。

| checkpoint / 後処理 | F1 | RR-MAE | RMSSD-MAE | SDNN-MAE |
|---|---|---|---|---|
| best_model(epoch3) / old | 0.671 | 11.56 ms | 23.17 ms | 6.71 ms |
| best_model(epoch3) / adaptive_searchback | 0.644 | 11.47 ms | 20.07 ms | 6.94 ms |
| epoch4 / old | **0.762** | 13.32 ms | 23.07 ms | 6.47 ms |
| epoch4 / adaptive_searchback | **0.765** | 13.12 ms | 25.66 ms | 8.33 ms |
| epoch7 / old | 0.731 | 12.57 ms | 24.60 ms | 7.11 ms |
| epoch7 / adaptive_searchback | 0.725 | 12.33 ms | 27.85 ms | 8.50 ms |
| **274(sigma=10、比較対象)** | 0.687 | **10.69 ms** | **16.27 ms** | (未計測) |

**F1はわずか8epochで0.76台まで到達し274(本番50epoch)の0.687を大きく上回る**。
これは狙い通り: Gaussianラベルを広げるとモデルの予測heatmapも真のR波周辺で
広く高い値を保つようになり、`match_peaks`(許容誤差50ms)内に極大点が入りやすく
なるため検出率(recall)が上がる。

**しかしRR-MAE・RMSSD-MAEはいずれも274より明確に悪化している**(RMSSD-MAEは
20〜28ms、274の16.27msを一度も下回れない)。これは検出(当たったかどうか)と
位置推定精度(いつ当たったか)のトレードオフとして理解できる: heatmapが広く
なるほど「50ms以内には入るが正確なピーク位置からはずれた」極大点が増え、
連続するRR間隔の差分に敏感なRMSSDのような指標を悪化させる。**F1という指標
単体は、sigmaを広げるだけで「見かけ上」改善できてしまう(ラベルが広いほど
許容誤差との整合が取りやすくなるため)ことを示す一例であり、F1だけで
後処理・前処理・ラベル設計を評価するのは危険だという教訓として重要**。

**結論**: sigma=20は現行の目的(RMSSD-MAE最重視)には適さないため棄却。
現行の生産用構成は引き続きconfig274(sigma=10、前処理なし)+
adaptive_searchbackとする。中間的なsigma(12〜15ms程度)であれば検出率と
位置精度の折衷点がある可能性はあるが、優先度は下げる(前処理・ラベル幅より、
後処理アルゴリズムまたはモデル自体の位置推定精度に踏み込む方が本質的な
改善につながる可能性が高い)。

## heatmap sigma中間値(15ms)プローブ: 有望、本番50epochへ昇格 (2026-08-07)

sigma=20msは検出率(F1)と位置推定精度(RR-MAE/RMSSD-MAE)のトレードオフを示したため、
10msと20msの中間であるsigma=15msを単一変数でプローブした(config283、8epoch)。

| checkpoint / 後処理 | F1 | RR-MAE | RMSSD-MAE | SDNN-MAE |
|---|---|---|---|---|
| epoch4 / old | 0.712 | 11.50 ms | **17.30 ms** | **5.22 ms** |
| epoch4 / adaptive_searchback | **0.722** | 11.56 ms | 19.15 ms | 6.03 ms |
| epoch7 / old | 0.704 | 12.50 ms | 22.14 ms | 7.34 ms |
| epoch7 / adaptive_searchback | 0.692 | 12.56 ms | 24.66 ms | 8.55 ms |
| **274(sigma=10、比較対象)** | 0.687 | **10.69 ms** | 16.27 ms | (未計測) |

sigma=20(RMSSD-MAE 20〜28ms)と比べ、sigma=15のepoch4はRMSSD-MAEの悪化がわずか
(+1.03ms)にとどまりながら、F1は0.687→0.712〜0.722まで明確に改善し、SDNN-MAEは
274より良好(5.22ms)。**検出率改善のメリットが位置精度低下のコストを上回る
折衷点として有望**と判断し、本番50epoch(config284、周期チェックポイント込み)へ
昇格させた。config281の教訓を踏まえ、完了後は全チェックポイントを下流タスクで
評価してから274との最終比較を行う(単一epochの結果だけで判断しない)。

## config284(sigma=15, 本番50epoch)結果: プローブの好結果は再現せず、274を維持 (2026-08-07)

config284(sigma=15, 50epoch, 周期チェックポイント込み)の全11チェックポイントを
`adaptive_searchback`で下流評価した:

| checkpoint | F1 | RR-MAE | RMSSD-MAE | SDNN-MAE |
|---|---|---|---|---|
| epoch4(最良) | 0.736 | 11.83 ms | 20.84 ms | 6.51 ms |
| epoch9〜49(9点) | 0.518〜0.688 | 11.30〜14.65 ms | 30.03〜41.88 ms | 11.29〜17.34 ms |
| **274(sigma=10、比較対象)** | 0.687 | **10.69 ms** | **16.27 ms** | (未計測) |

**この本番run全体を通して、どのepochも274のRMSSD-MAE(16.27ms)を下回れなかった**
(最良のepoch4でも20.84ms)。283の8epochプローブでは同じ「epoch4」チェックポイントで
RMSSD-MAE=17.30〜19.15msという274に迫る値が出ていたが、本番50epochの「epoch4」
では20.84msまで悪化しており、**"同じepoch数"のはずのチェックポイント間で無視できない
差が生じている**。学習ログを比較すると、epoch0/epoch4時点のtrain_loss・val_corrは
両run間でごくわずかにしか違わない(val_corr 0.242 vs 0.239等)。これはGPU上の
浮動小数点非決定性(cuDNNのアルゴリズム選択等、`torch.manual_seed`だけでは排除
できない)由来のノイズと考えられる。**heatmap検出の下流指標(F1・RMSSD-MAE)は、
この程度の微小な訓練差にも敏感に反応しうる**ということが分かった。

**結論**: sigma=15の8epochプローブで見えた改善は、本番50epochの同条件では
再現しなかった。1回のプローブ結果だけで本番判断をすると、この種の run-to-run
ノイズを実際の改善と誤認するリスクがある。**現行の生産用構成は引き続き
config274(sigma=10、前処理なし)+ adaptive_searchbackとする**。
heatmap sigma幅のチューニングはこれで一旦打ち切り、優先度を下げる。

## ecgshape neurokit本番30epoch: モード崩壊を確認、trivial baselineを上回れず (2026-08-07)

014の8epochプローブ(test_Corr 0.799〜0.857、trivial baseline 0.883未達)を受け、
本番epoch数(既定30)で再学習した(キュー023)。`evaluate_ecgshape_raw_cnn.py`の
崩壊診断(matched vs mismatched pairwise correlation)を使って評価した:

```
PRED-vs-PRED pairwise corr: mean=0.9172   (モデル出力同士の類似度、高いほど「いつも同じ形」)
TRUE-vs-TRUE pairwise corr: mean=0.7886   (正解同士の類似度、拍間の本来の多様性)
matched (pred_i vs true_i):    mean=0.8516
mismatched (pred_i vs random true_j): mean=0.8201
VERDICT: COLLAPSED
shapeMetric Corr=0.8487
```

**モード崩壊を確認した**: モデル出力同士の相関(0.917)が正解同士の相関(0.789)より
明確に高く、`matched`(正しいペア)と`mismatched`(ランダムなペア)の差がわずか
0.0315(診断スクリプトの崩壊判定しきい値0.05を下回る)しかない。**つまりこのモデルは
レーダ入力ごとに拍の形を作り分けられておらず、ほぼ「平均的な拍の形」を出力する
だけになっている**。全体のshapeMetric Corr(0.8487)も、progress_report内で
確立済みのtrivial baseline(「レーダ入力を一切見ずに学習データの平均的な拍の形を
出力するだけ」のモデルの相関0.883、III-8参照)を**依然として下回っている**。
本番epoch数に伸ばしても(8epochプローブの0.80台→30epochの0.85台への改善はあった
ものの)モード崩壊自体は解消しておらず、根本的な問題(単一拍の切り出し窓が短い・
入力SNRが低い・アーキテクチャの表現力不足など)が残っていると考えられる。
CLAUDE.md R5(モデルより先に入力と評価を疑う)の観点では、この崩壊は既にIII-8で
指摘されていた「相関という指標自体が平均形状で高く出てしまう」問題の具体的な
再確認であり、**単一のcorr値だけを見て精度向上と判断してはいけない**という
教訓を改めて裏付けた。この経路(単一拍波形回帰、`ecgshape`系列)は現状のアーキ
テクチャ・データ量では trivial baseline を安定して超えられておらず、優先度を
下げる。

## R+T統合教師の周期チェックポイント再検証: どのepoch・後処理でも274に届かず、正式に棄却確定 (2026-08-07)

277(R+T統合教師、本番50epoch)の「チェックポイント選択基準の乖離」という診断を実際に
検証していなかった(周期チェックポイント修正より前に学習されたrunだった)ため、
ユーザ指摘を受けて同一configをそのまま再学習(キュー024、`277_rt_full_rerun`)し、
全11チェックポイント×複数の後処理で下流評価した。

| checkpoint | 後処理 | F1 | RR-MAE | RMSSD-MAE | SDNN-MAE |
|---|---|---|---|---|---|
| epoch4 | old(閾値0.3) | 0.384 | 14.74 ms | 45.16 ms | 20.61 ms |
| epoch4 | paired_dedup | 0.397 | 14.67 ms | 46.44 ms | 22.64 ms |
| epoch4 | adaptive_searchback | 0.531 | 16.07 ms | 54.73 ms | 24.04 ms |
| epoch9 | old | 0.580 | 14.14 ms | 50.13 ms | 20.88 ms |
| epoch9 | paired_dedup | **0.636**(全組合せ中最良F1) | 14.25 ms | 43.80 ms | 17.28 ms |
| epoch9 | adaptive_searchback | 0.598 | 14.60 ms | 53.19 ms | 23.08 ms |
| epoch14〜34(5点) | adaptive_searchback | 0.299〜0.390 | 13.86〜17.94 ms | 61.32〜67.69 ms | 25.28〜30.16 ms |
| **274(比較対象)** | adaptive_searchback | 0.687 | **10.69 ms** | **16.27 ms** | (未計測) |

**どのepoch・どの後処理を組み合わせても274に遠く届かない**。全組合せ中の最良は
epoch9×paired_dedup(F1=0.636、RMSSD-MAE=43.80ms)で、F1こそ274の0.687に迫るが、
**RMSSD-MAEは274(16.27ms)の2.7倍悪い**。`old`/`paired_dedup`(R+Tのペア構造を
意識した設計)は`adaptive_searchback`(意識しない汎用アルゴリズム)より一貫して
良い傾向があり、後処理側の工夫がR+T方式の欠点を部分的に緩和することは確認できた
ものの、いずれの組み合わせも274の水準には遠く及ばない。チェックポイント選択の
問題ではなく、**R+T統合教師というアイデア自体がR単独教師に対して一貫して劣る**
ことを、今回初めて公平な条件(周期
チェックポイント、複数後処理)で確認できた。R+T統合教師は正式に棄却する。

## R波heatmapの評価低下窓の視覚診断とタイムラグ補正の検証 (2026-08-07、ユーザ指摘を受けて)

ユーザから2つの指摘を受けた: (1) 274/R+Tいずれの最悪窓画像でも、モデルの予測ピークと
正解Rの位置に0.1〜0.2秒ほどのズレが見える、補正できないか。(2) sigma=20msでF1が
改善しつつRMSSD-MAEが悪化するのは原理的におかしい、極大点選択アルゴリズムの問題では
ないか。詳しく調べた。

**手順上のミスの訂正**: 最初に作成した診断スクリプト(`plot_worst_heatmap_windows.py`
等)は`extract_peaks_adaptive_searchback`を呼ぶ際にチューニング済みパラメータ
(`candidate_height=0.20, threshold_frac=0.8, searchback_threshold_frac=0.6`)を
渡し忘れ、関数自体のデフォルト(文献のPan-Tompkins既定値、`candidate_height=0.05,
threshold_frac=0.25`)で動いていた。これによりFP率が異常に高い(81.6%)結果になり、
最初に生成した`worst_windows_274.png`/`worst_windows_277_rt.png`のオレンジ破線
(選択された極大点)が実際の本番後処理よりずっと過検出気味になっていた
(ノイズフロアの小さな凸凹まで多数選択していた)。全診断スクリプトを修正し、
FP/FN集計はチューニング済みパラメータでの数値(config274: FP 35.1%/FN 30.6%、
既報のprecision=0.683/recall=0.705と整合)に置き換えた。**視覚化した画像自体は
未修正版のままなので、オレンジ破線の密度は本番の実際の後処理より過大に見える点に
注意**(赤線=正解Rの位置、真の予測heatmap自体の形状は正しい)。

**(2)への回答: 極大点選択アルゴリズムの問題ではない**。`diagnose_localization_error.py`
で「heatmap近傍±150msの極大点のうちargmaxを選んだ場合(理論上の最良選択)」と
「実際の後処理が選んだ場合」を比較したところ、sigma=10で(b)-(a)=-0.78ms、
sigma=20でも同じく-0.78msと、**後処理は理論上の最良選択にほぼ届いており、
選択アルゴリズム自体に大きな改善余地は無い**(ユーザの仮説とは逆の結果)。
むしろsigma=20の方が近傍の極大点数(19.98→12.64)・offset MAE(37.03→34.38ms)
とも改善しており、個々のピーク単体の位置精度はsigma=20の方がわずかに良い。
にもかかわらずRMSSD-MAEが悪化する理由は、個々のピークの位置精度ではなく、
FP/FN(見逃し・過検出)が予測RR系列に混入させる不連続なジャンプの影響と考えられる
(今回は時間切れで定量的な追跡までは行っていない、今後の課題)。

**(1)への回答: 系統的なタイムラグ補正は効果が無い、原理的な理由がある**。
`diagnose_and_correct_time_lag.py`でconfig274の全マッチ済みペア(n=9616)の
「予測ピーク位置 - 正解R位置」を集計したところ、**mean=6.54ms, median=15.00ms,
std=28.83ms**であり、視覚的に見えた「0.1〜0.2秒」ほど大きな系統的ラグは
データセット全体では確認できなかった(ユーザが見た最悪ケースの窓だけがたまたま
大きくズレていた可能性が高い)。さらに、-100ms〜+100msでシフト量を振った
グリッドサーチでは、**F1はshift=0msで最大(0.687)となり、シフトを加えると
むしろ悪化する**(shift=+15ms(median補正)でF1=0.661、shift=-15msでも同様に悪化)。
**RMSSD-MAE/SDNN-MAEに至っては、どのシフト量でもほぼ完全に不変(20.71ms固定、
±100msの極端なシフトでも20.49〜20.71msの範囲)**。これは数学的に当然の帰結:
RMSSDは連続するRR Interval同士の差分に基づく指標であり、全ピークに同じ定数を
足し引きしても連続差分(RR Interval自体)は変化しないため、**定数シフト補正は
原理的にRMSSD-MAEを改善できない**。RMSSD-MAEを下げるには、ピーク位置全体を
一律にずらすのではなく、拍ごとに異なる(FP/FN起因の非一様な)誤差を減らす必要が
あり、これは前述のFP/FN対策(閾値・後処理の再設計)の方向性でしか解決しない。

## 重大な訂正: F1とRR-MAE/RMSSD-MAEの取り違えを修正 (2026-08-07、ユーザ指摘を受けて)

本ファイル内で複数回「274(前処理なし)+adaptive_searchback = F1=0.687, RR-MAE=10.69ms,
RMSSD-MAE=16.27ms」と記載していたが、**これは誤りだった**。RR-MAE=10.69ms/
RMSSD-MAE=16.27msは実際には「旧手法(閾値0.3)」(F1=0.551)の数値であり、
adaptive_searchback(F1=0.687)のRR-MAEは12.00ms、RMSSD-MAEは一度も測定されて
いなかった(`compare_peak_postprocessing.py`はF1とRR-MAEしか計算していなかった)。
2つの後処理の数値を混同したまま「確定構成」として何度も報告し、config281/282/283/284/277
の棄却判断もこの誤った基準(16.27ms)と比較していた。改めてconfig274+adaptive_searchbackを
測定した結果、正しい数値は**F1=0.687, RR-MAE=12.00ms, RMSSD-MAE=20.71ms, SDNN-MAE=6.51ms**。
これにより「F1を優先するか、RMSSD-MAEを優先するか」で採用すべき後処理が変わるという
トレードオフが判明した(下記ランキング参照)。過去の棄却判断(281/282/283/284/277)は
正しい基準(20.71ms)で比較しても結論は変わらない(いずれも20.71msを上回るか、
上回らなくても僅差でF1が低い)ことを確認済み。

## 全候補モデルの統一評価ランキング (2026-08-07、ユーザ依頼)

評価指標の設計を明確化した: **RR-MAE**(`matched_rr_interval_mae_ms`)は真の隣接
R波2つが両方ともマッチできた区間だけを比較する設計で、意図的に見逃し・偽陽性を
除外している(QRS検出分野の標準的な二段評価: 検出性能=F1、位置精度=正しく検出できた
場合のRR-MAE、に近い)。**RMSSD-MAE**はモデルの実際の予測ピーク列全体から計算する
ため見逃し・偽陽性の影響を受けるが、標準的なHRVアーチファクト除去フィルタ
(`clean_rr_intervals_ms`、40〜200bpm範囲外・局所中央値から20%以上ずれるRR間隔を除去)
を通すため、一部の異常値は緩和される。また有効な間隔が3つ未満のトライアルは
「HRV usable」から除外され平均に入らない(今回の全評価では44/44が使用可能)。

統一スクリプト(`evaluate_spatial_gnn_hrv.py`、チューニング済みパラメータ)で
これまでの主要チェックポイントを再評価し、RMSSD-MAE昇順でランキングした:

| 順位 | モデル | GT | 後処理 | F1 | RR-MAE | RMSSD-MAE | SDNN-MAE |
|---|---|---|---|---|---|---|---|
| 1 | 274(sigma10) | neurokit | old | 0.551 | 10.69ms | **16.27ms** | 6.53ms |
| 2 | 283 epoch4(sigma15プローブ) | neurokit | old | 0.712 | 11.50ms | 17.30ms | 5.22ms |
| 3 | 284 epoch4(sigma15本番) | neurokit | old | **0.733** | 11.76ms | 18.26ms | **5.16ms** |
| 4 | 283 epoch4 | neurokit | adaptive_searchback | 0.722 | 11.56ms | 19.15ms | 6.03ms |
| 5 | 281 epoch4(channel_weight) | neurokit | old | 0.670 | 11.33ms | 19.31ms | 6.62ms |
| 6 | **2モデル融合: 274(密候補,h=0.20)+284 epoch4** | neurokit | 2model_fusion | 0.729 | 12.03ms | 19.77ms | 5.93ms |
| 7 | 274 | neurokit | adaptive_searchback | 0.687 | 12.00ms | 20.71ms | 6.51ms |
| 8 | 284 epoch4 | neurokit | adaptive_searchback | 0.736 | 11.83ms | 20.84ms | 6.51ms |
| 9 | config236(旧ラベル) | legacy | old | 0.497 | 10.34ms | 21.43ms | 8.95ms |
| 10 | config236 | legacy | adaptive_searchback | 0.662 | 11.92ms | 22.37ms | 7.35ms |
| 11 | 282 epoch4(sigma20) | neurokit | old | 0.762 | 13.32ms | 23.07ms | 6.47ms |
| 12 | 2モデル融合: 274+282(h=0.20) | neurokit | 2model_fusion | 0.760 | 12.48ms | 23.40ms | 7.30ms |
| 13 | 281 epoch4 | neurokit | adaptive_searchback | 0.700 | 11.71ms | 23.70ms | 8.52ms |
| 14 | 282 epoch4(F1最高) | neurokit | adaptive_searchback | **0.765** | 13.12ms | 25.66ms | 8.33ms |
| 15 | R+T epoch9(棄却済) | neurokit_rt | paired_dedup | 0.636 | 14.25ms | 43.80ms | 17.28ms |

**RR-MAEは歴史的な最良値9.67ms(config236)を今回どの構成も更新できていない**。
なお9.67ms自体も今回同一checkpointで再現できず(10.34ms、評価スクリプトのversion差
と推測、R3の教訓)。一方**F1は0.383/0.497(旧ラベル)から0.71〜0.77台まで大幅に改善**。

**進捗報告書更新の推奨候補: config284 epoch4 + old手法(3位)**。RMSSD-MAEが最良値
(16.27ms)にわずか2ms差(18.26ms)まで迫りながら、F1は0.551→0.733まで大幅改善、
SDNN-MAEは全候補中最良(5.16ms)。8epochプローブ(283)と50epoch本番(284)でほぼ同じ
結果(17.3ms/18.3ms)が出ており安定して再現している(以前「sigma15はプローブが
再現しなかった」と報告したのはadaptive_searchbackとの組み合わせでの話で、old手法との
組み合わせでは実際には安定していたと訂正する)。

## 2モデル融合案の検証 (2026-08-07、ユーザ発案)

「誤差が一番小さいモデル(model_A)が出す多数の検出点候補(precisionは低くてよい)の
うち、F1が高いモデル(model_B)の検出位置に一番近いものを選択する」という後処理案。
`diagnose_two_model_fusion.py`で実装し検証した。

初回(model_A=旧ラベルconfig236、model_B=282 epoch4、候補閾値0.05固定)は
ほぼ無効果(F1 0.765→0.767、RMSSD-MAE 25.66→26.69msとむしろ悪化)だった。
ユーザ指摘によりmodel_A側の候補閾値(candidate_height_a)を掃引したところ、
**閾値の選び方で結果が大きく変わることを確認**した:

| candidate_height_a | 274+282融合 F1/RMSSD-MAE | 274+284融合 F1/RMSSD-MAE |
|---|---|---|
| 0.02〜0.10(低すぎ) | 0.755 / 27.71ms(悪化) | 0.727 / 23.14ms(悪化) |
| 0.15 | 0.756 / 26.64ms | 0.726 / 22.15ms |
| **0.20** | **0.760 / 23.40ms(改善)** | **0.729 / 19.77ms(改善)** |
| 0.30 | 0.750 / 25.01ms | 0.725 / 20.40ms |
| 0.40 | 0.763 / 25.45ms | 0.734 / 21.04ms |

model_A(274)を低RMSSD-MAEの供給元、model_B(282/284)を高F1の供給元とする組み合わせで、
**candidate_height_a=0.20が一貫して最良**(閾値が低すぎるとノイズフロアの候補を
拾って悪化する)。274+284融合(h=0.20)はF1=0.729/RMSSD-MAE=19.77msとなり、
上記ランキング6位に入る結果となった。ただしいずれの融合結果も274単体
(旧手法、RMSSD-MAE=16.27ms)は上回れていない。

## 未解決・今後の課題

- Anchor CNNのneurokitラベルでの悪化は未解決(weight_decayでは改善せず)。
  優先度を下げて保留。空間GNN(274)を当面の主力モデルとする。
- R+T統合教師は周期チェックポイント込みで再検証した結果、正式に棄却確定
  (上記参照、再検証の予定なし)。
- 評価の低い窓の視覚化画像(`worst_windows_274.png`/`worst_windows_277_rt.png`)は
  未チューニングパラメータで生成されたものなので、必要なら
  `plot_worst_heatmap_windows.py`(修正済み)で再生成すること。
- RMSSD-MAE改善の残された方向性: FP/FN(見逃し・過検出)が予測RR系列に与える
  影響の定量分析(どのFP/FNがRMSSDを最も悪化させているか)。時間切れで未着手。
- `ecgshape`(波形回帰)モデルはneurokit本番30epochでもモード崩壊が続きtrivial
  baselineを超えられず(上記参照)。優先度を下げる。
- バンドパスフィルタのheatmapパイプラインでの単一変数プローブ(既に278で棄却済み)。
- 2モデル融合は274+284(h=0.20)がランキング6位まで改善したが、274単体(旧手法)は
  上回れていない。他のmodel_A/model_B組み合わせ・snap_window_msの調整余地あり。
- 方法論上の教訓: 8epochプローブ結果が本番50epochの同epoch時点で再現しない
  ケースを確認した(GPU非決定性由来と推測)。今後、プローブの結果が僅差の場合は
  「見込みあり」と即断せず、複数seedまたは本番run側での早期評価で再確認する
  運用を検討する余地がある。
- F1だけでなくRR-MAE/RMSSD-MAEを常に併記する運用は今後も徹底する。
- 現時点での最有力候補: config284 epoch4 + old手法(F1=0.733, RR-MAE=11.76ms,
  RMSSD-MAE=18.26ms, SDNN-MAE=5.16ms)。progress_report更新はユーザ確認待ち。
  ecgshape(波形形状)・Anchor CNNはいずれも未解決課題を抱えたまま優先度を下げて
  保留し、本セッションのR波heatmap関連調査はここで一区切りとする。
